# for setting up rpi gpio
# see https://askubuntu.com/questions/1230947/gpio-for-raspberry-pi-gpio-group 
sudo apt-get install rpi.gpio-common

# for setting up gpio as well
# suggested by chatgpt
sudo apt install -y gpiod libgpiod-dev

# for setting up spi
sudo apt-get install wringPi // I don't know why but wiringPi installed in this way may have issue when calling its lib
alternatively, download and install from https://github.com/WiringPi/WiringPi, please check the repo's README for more information
